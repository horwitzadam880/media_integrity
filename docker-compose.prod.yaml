# docker-compose.prod.yml
services:
  app:
    image: ghcr.io/horwitzadam880/media_integrity:${IMAGE_TAG}
    environment:
      - ENVIRONMENT=production
      - GDRIVE_KEY=${GDRIVE_KEY}
    ipc: host
    init: true
